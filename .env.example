# The template itself does not require environment variables.
# Copy this file to `.env` only after your derived project introduces runtime configuration.

# Example placeholders for downstream projects:
# APP_ENV=development
# DATABASE_URL=
# API_BASE_URL=
